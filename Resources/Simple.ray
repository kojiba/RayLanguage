#include <stdio.h>

class Simple {
properties:
    int a, b;   // readonly by default
methods:
    printA;
    printB;
}

implemantation Simple {
    printA {
        printf("A is - %d", object.a);
    }
    printfB {
        printf("B is - %d", object.b);
    }
}